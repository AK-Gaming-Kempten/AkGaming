@inject HttpClient Api

<div class="two-column-layout mobile-master-detail @(_isMobileDetailOpen ? "mobile-detail-open" : string.Empty)">
    <div class="two-column-layout-left">
        <div class="list-buttons">
            <h4>Pending Requests</h4>
            <ActionButton Kind="ActionButtonKind.Filter" OnClick="OpenFilters" Disabled="true" Label="Filter requests" />
        </div>
        <div class="list-main">
            @if (_requests == null) {
                <p>Loading…</p>
            }
            else if (!_requests.Any()) {
                <p>No requests found.</p>
            }
            else {
                <div class="list-scroll-container">
                    <div class="preview-card-list">
                        @foreach (var request in _requests) {
                            <ApplicationRequestPreviewCard Request="request" IsSelected="request == _selectedRequest" OnSelect="SelectRequest"/>
                        }
                    </div>
                </div>
            }
        </div>
    </div>
    <div class="two-column-layout-right">
        <div class="mobile-detail-nav">
            <ActionButton Kind="ActionButtonKind.Back" Variant="ActionButtonVariant.Outline" Size="ActionButtonSize.Small" OnClick="ShowListMobile" Label="Back to requests" />
        </div>
        <CascadingValue Value="MemberApi" Name="MemberManagementApi" IsFixed="true">
            <ApplicationRequestCard IsEditable="CanManage" Request="_selectedRequest" OnRequestUpdated="Reload" />
        </CascadingValue>  
    </div>
</div>
