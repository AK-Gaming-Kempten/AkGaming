@page "/tournaments/{TournamentSlug}/player/check-in"
@namespace AkGaming.Tournaments.Frontend.Components.Tournaments.Player
@attribute [Authorize]
@inherits TournamentPageBase

<PageTitle>@($"{Tournament?.Name ?? "Tournament"} Check-In")</PageTitle>

<section class="app-page">
    <PageHeader Title="Check-in" />

    <SurfacePanel Title="Check-in">
        <p class="muted-copy">Check-in workflow is TBA.</p>
    </SurfacePanel>
</section>
